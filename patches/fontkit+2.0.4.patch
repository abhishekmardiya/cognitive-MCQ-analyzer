diff --git a/node_modules/fontkit/dist/browser-module.mjs b/node_modules/fontkit/dist/browser-module.mjs
index 4d0a389..0e7b3de 100644
--- a/node_modules/fontkit/dist/browser-module.mjs
+++ b/node_modules/fontkit/dist/browser-module.mjs
@@ -9938,29 +9938,57 @@ class $c96c93587d49c14d$export$2e2bcd8739ae039 extends (0, $a83b9c36aaa94fd3$exp
         }
     }
     applyAnchor(markRecord, baseAnchor, baseGlyphIndex) {
+        if (markRecord == null) {
+            return;
+        }
         let baseCoords = this.getAnchor(baseAnchor);
         let markCoords = this.getAnchor(markRecord.markAnchor);
         let basePos = this.positions[baseGlyphIndex];
         let markPos = this.positions[this.glyphIterator.index];
+        if (basePos == null || markPos == null) {
+            return;
+        }
         markPos.xOffset = baseCoords.x - markCoords.x;
         markPos.yOffset = baseCoords.y - markCoords.y;
         this.glyphIterator.cur.markAttachment = baseGlyphIndex;
     }
     getAnchor(anchor) {
-        // TODO: contour point, device tables
-        let x = anchor.xCoordinate;
-        let y = anchor.yCoordinate;
-        // Adjustments for font variations
-        let variationProcessor = this.font._variationProcessor;
-        let variationStore = this.font.GDEF && this.font.GDEF.itemVariationStore;
-        if (variationProcessor && variationStore) {
-            if (anchor.xDeviceTable) x += variationProcessor.getDelta(variationStore, anchor.xDeviceTable.a, anchor.xDeviceTable.b);
-            if (anchor.yDeviceTable) y += variationProcessor.getDelta(variationStore, anchor.yDeviceTable.a, anchor.yDeviceTable.b);
+        try {
+            // TODO: contour point, device tables
+            if (anchor == null) {
+                return {
+                    x: 0,
+                    y: 0
+                };
+            }
+            let x = anchor.xCoordinate;
+            let y = anchor.yCoordinate;
+            let variationProcessor = this.font._variationProcessor;
+            let variationStore = this.font.GDEF && this.font.GDEF.itemVariationStore;
+            if (variationProcessor && variationStore) {
+                if (anchor.xDeviceTable) {
+                    const xdt = anchor.xDeviceTable;
+                    if (xdt != null && xdt.a != null && xdt.b != null) {
+                        x += variationProcessor.getDelta(variationStore, xdt.a, xdt.b);
+                    }
+                }
+                if (anchor.yDeviceTable) {
+                    const ydt = anchor.yDeviceTable;
+                    if (ydt != null && ydt.a != null && ydt.b != null) {
+                        y += variationProcessor.getDelta(variationStore, ydt.a, ydt.b);
+                    }
+                }
+            }
+            return {
+                x: x,
+                y: y
+            };
+        } catch {
+            return {
+                x: 0,
+                y: 0
+            };
         }
-        return {
-            x: x,
-            y: y
-        };
     }
     applyFeatures(userFeatures, glyphs, advances) {
         super.applyFeatures(userFeatures, glyphs, advances);
diff --git a/node_modules/fontkit/dist/browser.cjs b/node_modules/fontkit/dist/browser.cjs
index a5fa901..0874f28 100644
--- a/node_modules/fontkit/dist/browser.cjs
+++ b/node_modules/fontkit/dist/browser.cjs
@@ -9955,29 +9955,57 @@ class $79ea6270f0a90256$export$2e2bcd8739ae039 extends (0, $7b226e6bbeadedeb$exp
         }
     }
     applyAnchor(markRecord, baseAnchor, baseGlyphIndex) {
+        if (markRecord == null) {
+            return;
+        }
         let baseCoords = this.getAnchor(baseAnchor);
         let markCoords = this.getAnchor(markRecord.markAnchor);
         let basePos = this.positions[baseGlyphIndex];
         let markPos = this.positions[this.glyphIterator.index];
+        if (basePos == null || markPos == null) {
+            return;
+        }
         markPos.xOffset = baseCoords.x - markCoords.x;
         markPos.yOffset = baseCoords.y - markCoords.y;
         this.glyphIterator.cur.markAttachment = baseGlyphIndex;
     }
     getAnchor(anchor) {
-        // TODO: contour point, device tables
-        let x = anchor.xCoordinate;
-        let y = anchor.yCoordinate;
-        // Adjustments for font variations
-        let variationProcessor = this.font._variationProcessor;
-        let variationStore = this.font.GDEF && this.font.GDEF.itemVariationStore;
-        if (variationProcessor && variationStore) {
-            if (anchor.xDeviceTable) x += variationProcessor.getDelta(variationStore, anchor.xDeviceTable.a, anchor.xDeviceTable.b);
-            if (anchor.yDeviceTable) y += variationProcessor.getDelta(variationStore, anchor.yDeviceTable.a, anchor.yDeviceTable.b);
+        try {
+            // TODO: contour point, device tables
+            if (anchor == null) {
+                return {
+                    x: 0,
+                    y: 0
+                };
+            }
+            let x = anchor.xCoordinate;
+            let y = anchor.yCoordinate;
+            let variationProcessor = this.font._variationProcessor;
+            let variationStore = this.font.GDEF && this.font.GDEF.itemVariationStore;
+            if (variationProcessor && variationStore) {
+                if (anchor.xDeviceTable) {
+                    const xdt = anchor.xDeviceTable;
+                    if (xdt != null && xdt.a != null && xdt.b != null) {
+                        x += variationProcessor.getDelta(variationStore, xdt.a, xdt.b);
+                    }
+                }
+                if (anchor.yDeviceTable) {
+                    const ydt = anchor.yDeviceTable;
+                    if (ydt != null && ydt.a != null && ydt.b != null) {
+                        y += variationProcessor.getDelta(variationStore, ydt.a, ydt.b);
+                    }
+                }
+            }
+            return {
+                x: x,
+                y: y
+            };
+        } catch {
+            return {
+                x: 0,
+                y: 0
+            };
         }
-        return {
-            x: x,
-            y: y
-        };
     }
     applyFeatures(userFeatures, glyphs, advances) {
         super.applyFeatures(userFeatures, glyphs, advances);
diff --git a/node_modules/fontkit/dist/main.cjs b/node_modules/fontkit/dist/main.cjs
index 851b2d2..b482fe8 100644
--- a/node_modules/fontkit/dist/main.cjs
+++ b/node_modules/fontkit/dist/main.cjs
@@ -9976,29 +9976,57 @@ class $79ea6270f0a90256$export$2e2bcd8739ae039 extends (0, $7b226e6bbeadedeb$exp
         }
     }
     applyAnchor(markRecord, baseAnchor, baseGlyphIndex) {
+        if (markRecord == null) {
+            return;
+        }
         let baseCoords = this.getAnchor(baseAnchor);
         let markCoords = this.getAnchor(markRecord.markAnchor);
         let basePos = this.positions[baseGlyphIndex];
         let markPos = this.positions[this.glyphIterator.index];
+        if (basePos == null || markPos == null) {
+            return;
+        }
         markPos.xOffset = baseCoords.x - markCoords.x;
         markPos.yOffset = baseCoords.y - markCoords.y;
         this.glyphIterator.cur.markAttachment = baseGlyphIndex;
     }
     getAnchor(anchor) {
-        // TODO: contour point, device tables
-        let x = anchor.xCoordinate;
-        let y = anchor.yCoordinate;
-        // Adjustments for font variations
-        let variationProcessor = this.font._variationProcessor;
-        let variationStore = this.font.GDEF && this.font.GDEF.itemVariationStore;
-        if (variationProcessor && variationStore) {
-            if (anchor.xDeviceTable) x += variationProcessor.getDelta(variationStore, anchor.xDeviceTable.a, anchor.xDeviceTable.b);
-            if (anchor.yDeviceTable) y += variationProcessor.getDelta(variationStore, anchor.yDeviceTable.a, anchor.yDeviceTable.b);
+        try {
+            // TODO: contour point, device tables
+            if (anchor == null) {
+                return {
+                    x: 0,
+                    y: 0
+                };
+            }
+            let x = anchor.xCoordinate;
+            let y = anchor.yCoordinate;
+            let variationProcessor = this.font._variationProcessor;
+            let variationStore = this.font.GDEF && this.font.GDEF.itemVariationStore;
+            if (variationProcessor && variationStore) {
+                if (anchor.xDeviceTable) {
+                    const xdt = anchor.xDeviceTable;
+                    if (xdt != null && xdt.a != null && xdt.b != null) {
+                        x += variationProcessor.getDelta(variationStore, xdt.a, xdt.b);
+                    }
+                }
+                if (anchor.yDeviceTable) {
+                    const ydt = anchor.yDeviceTable;
+                    if (ydt != null && ydt.a != null && ydt.b != null) {
+                        y += variationProcessor.getDelta(variationStore, ydt.a, ydt.b);
+                    }
+                }
+            }
+            return {
+                x: x,
+                y: y
+            };
+        } catch {
+            return {
+                x: 0,
+                y: 0
+            };
         }
-        return {
-            x: x,
-            y: y
-        };
     }
     applyFeatures(userFeatures, glyphs, advances) {
         super.applyFeatures(userFeatures, glyphs, advances);
diff --git a/node_modules/fontkit/dist/module.mjs b/node_modules/fontkit/dist/module.mjs
index fde88fc..e4c472b 100644
--- a/node_modules/fontkit/dist/module.mjs
+++ b/node_modules/fontkit/dist/module.mjs
@@ -9959,29 +9959,57 @@ class $c96c93587d49c14d$export$2e2bcd8739ae039 extends (0, $a83b9c36aaa94fd3$exp
         }
     }
     applyAnchor(markRecord, baseAnchor, baseGlyphIndex) {
+        if (markRecord == null) {
+            return;
+        }
         let baseCoords = this.getAnchor(baseAnchor);
         let markCoords = this.getAnchor(markRecord.markAnchor);
         let basePos = this.positions[baseGlyphIndex];
         let markPos = this.positions[this.glyphIterator.index];
+        if (basePos == null || markPos == null) {
+            return;
+        }
         markPos.xOffset = baseCoords.x - markCoords.x;
         markPos.yOffset = baseCoords.y - markCoords.y;
         this.glyphIterator.cur.markAttachment = baseGlyphIndex;
     }
     getAnchor(anchor) {
-        // TODO: contour point, device tables
-        let x = anchor.xCoordinate;
-        let y = anchor.yCoordinate;
-        // Adjustments for font variations
-        let variationProcessor = this.font._variationProcessor;
-        let variationStore = this.font.GDEF && this.font.GDEF.itemVariationStore;
-        if (variationProcessor && variationStore) {
-            if (anchor.xDeviceTable) x += variationProcessor.getDelta(variationStore, anchor.xDeviceTable.a, anchor.xDeviceTable.b);
-            if (anchor.yDeviceTable) y += variationProcessor.getDelta(variationStore, anchor.yDeviceTable.a, anchor.yDeviceTable.b);
+        try {
+            // TODO: contour point, device tables
+            if (anchor == null) {
+                return {
+                    x: 0,
+                    y: 0
+                };
+            }
+            let x = anchor.xCoordinate;
+            let y = anchor.yCoordinate;
+            let variationProcessor = this.font._variationProcessor;
+            let variationStore = this.font.GDEF && this.font.GDEF.itemVariationStore;
+            if (variationProcessor && variationStore) {
+                if (anchor.xDeviceTable) {
+                    const xdt = anchor.xDeviceTable;
+                    if (xdt != null && xdt.a != null && xdt.b != null) {
+                        x += variationProcessor.getDelta(variationStore, xdt.a, xdt.b);
+                    }
+                }
+                if (anchor.yDeviceTable) {
+                    const ydt = anchor.yDeviceTable;
+                    if (ydt != null && ydt.a != null && ydt.b != null) {
+                        y += variationProcessor.getDelta(variationStore, ydt.a, ydt.b);
+                    }
+                }
+            }
+            return {
+                x: x,
+                y: y
+            };
+        } catch {
+            return {
+                x: 0,
+                y: 0
+            };
         }
-        return {
-            x: x,
-            y: y
-        };
     }
     applyFeatures(userFeatures, glyphs, advances) {
         super.applyFeatures(userFeatures, glyphs, advances);
